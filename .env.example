# Configuración de Docker Compose para proyecto-FoodManager
# Copiar este archivo como .env y ajustar los valores según sea necesario

# Base de datos MySQL
DB_ROOT_PASSWORD=rootpassword
DB_USER=admin
DB_PASSWORD=adminpassword
DB_NAME=supermercado

# Backend - JWT Secret
# IMPORTANTE: Generar uno nuevo para producción con:
# openssl rand -hex 64
JWT_SECRET=change-this-in-production-use-openssl-rand-hex-64-to-generate-secure-secret

# Puertos expuestos (cambiar si hay conflictos)
# MYSQL_PORT=3306
# BACKEND_PORT=3000
# FRONTEND_PORT=8080
